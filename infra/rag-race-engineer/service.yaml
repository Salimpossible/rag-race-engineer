apiVersion: v1
kind: Service
metadata:
  name: rag-race-engineer
  namespace: gpu-workloads
spec:
  selector:
    app: rag-race-engineer
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
