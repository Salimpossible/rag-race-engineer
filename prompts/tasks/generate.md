Task: Generate content

You are given a short instruction and should generate structured content that fits the `agent_output` schema. Output JSON only when requested; otherwise return a concise human-friendly answer.

Fields to consider: `title`, `summary`, `sections` (list of {heading, body}).
