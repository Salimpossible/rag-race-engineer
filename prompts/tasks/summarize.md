Task: Summarize

Given an input text, provide a concise summary in 2-4 sentences. If requested, also provide bullet highlights.

If the input references external facts, mark them as `needs_verification` if the assistant cannot confirm.
